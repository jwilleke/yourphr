# CLAUDE.md

__Canonical agent brief:__ [`AGENTS.md`](AGENTS.md).

- __Session protocol__ (kit-managed): the block between `KIT:START` / `KIT:END` in `AGENTS.md`.
- __Product context__ (this repo): everything in `AGENTS.md` __below__ `KIT:END` — mission, PHI rules, commands, architecture, deploy, conventions.

This file exists so Claude Code and other tools that auto-load `CLAUDE.md` still land on guidance. __Do not__ maintain a second copy of architecture or PHI rules here; edit `AGENTS.md` below `KIT:END` only.
