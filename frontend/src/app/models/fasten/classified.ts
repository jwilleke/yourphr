// Re-exported from the lib model so the cross-cutting Classified type has a single definition
// (the FastenDisplayModel base — in lib — must not depend on app/). See lib/models/fasten/classified.ts.
export {Classified} from '../../../lib/models/fasten/classified';
